/// V2 mozaik brand kit — shared widgets that carry the new visual
/// identity (deep navy + coral + teal + olive) across the app.
///
/// Pair these with `V2Typography` from
/// `package:lifeclient/product/init/application_theme.dart` for the
/// DM Serif Display + Plus Jakarta Sans typography pairing.
library;

export 'eyebrow.dart';
export 'hy_logo.dart';
export 'mosaic_grid.dart';
export 'status_pill.dart';
